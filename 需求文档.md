TOT26大作业C++
矢量图AI标注工具
浏览和标注矢量图（DXF格式）的PC端工具。
术语
●DXF（Drawing Exchange Format）： Autodesk 推出的CAD 通用矢量交换文件格式，是DWG的公开交换版本；参考文件：https://paulbourke.net/dataformats/dxf/ 
○组码：DXF 最小数据单元标识，两行一组（数字编码 + 对应值），决定数据类型与业务含义，是解析所有对象的基础。
○数据段：DXF 顶层数据分区（HEADER/TABLES/BLOCKS/ENTITIES），按固定顺序存储全局参数、资源定义、图形数据。
○实体：ENTITIES 段与布局块内最小绘图单元（直线、圆等），携带坐标、图层、空间归属等全套组码属性。
○图层：TABLES 段 LAYER 子表存储的样式分类资源，实体通过组码 8的图层名关联图层颜色、线型、显隐属性。
○块：BLOCKS 段存储的复用图形模板，INSERT 实体通过组码 2块名引用块内成套实体；*Model_Space/*Paper_Space 为空间专用特殊块。
○空间（模型 / 布局）：图纸两大逻辑分区，实体通过组码 67区分归属（0 = 模型空间，1 = 布局空间），新版非激活布局图形存于对应 * Paper_SpaceN 块中。
●实例标注（Instance Annotation）：逐个标注图纸中每一个具体对象个体，比如一个沙发、一扇门；
●类型标注（Type Annotation）：标注目标物体所属类别，不区分个体；
界面布局

菜单栏
文件
●打开（Ctrl+O）
●------------------
●保存（Ctrl+S）
●关闭（Ctrl+F4）
●------------------
●最近打开文件列表
●------------------
●退出
编辑
●撤销（Ctrl+Z）
●重复（Ctrl+Y）
●------------------
●类型标注
●实例标注
视图
●界面可见性控制
●------------------
●显示全图
●实时缩放
●窗口缩放
●实时平移
帮助
●帮助
●关于
工具栏
文件
●打开（Ctrl+O）
●保存（Ctrl+S）
编辑
●撤销（Ctrl+Z）
●重复（Ctrl+Y）
●------------------
●类型标注
●实例标注
视图
●显示全图
●实时缩放
●窗口缩放
●实时平移
面板区
面板区可泊靠类型、实例和图层三个列表界面。和渲染区之间的分隔可左右拖动调整大小。
类型
界面如下图所示，列表上方有一工具栏。

工具栏共有四个按钮功能：
●新增：新增类型记录；
●删除：删除当前光标所在的类型记录；如果列表为空，应灰显不可用。
●上移：将当前类型与上面的类型记录交换位置；如果上面没有类型记录，应灰显不可用。
●下移：将当前类型与下面的类型记录交换位置；如果下面没有类型记录，应灰显不可用。
类型列表用表格来展现，表格共分三列：
●类型名
●颜色：标注颜色，用当前类型标注CAD实体后，该实体颜色改为此标注颜色；颜色修改时，应同步将所有该类型标注的实体颜色修改。
●数量：标注的CAD实体数量。
实例
界面如下图所示，列表上方有一工具栏。

工具栏共有两个按钮功能：
●新增：新增实例记录；
●删除：删除当前光标所在的实例记录；如果列表为空，应灰显不可用。
实例列表用树来展现，共分两级：
●第一级节点：显示当前类型名称和当前类型下实例数量，格式：名称(数量)；
●第二级节点：显示实例名；
图层
用表格来展示，共有5列：
●名称：图层名称；只读
●颜色：图层颜色；只读
●线宽：图层线宽；只读
●线型：图层线型；只读
●可见性：勾选框，可修改。勾选，当前图层下所有CAD实体全部显示；不勾选，则不显示；
图层界面默认不显示，通过菜单中【界面可见性控制】来显隐。
图形渲染区
用于渲染DXF图纸，展示标注数据，界面如下图所示。

状态栏
共分四栏，分别显示：
●打开的DXF文件路径；
●CAD实体数量；
●类型标注数量；
●实例标注数量；
功能需求
序号	分类	功能名	功能描述	分数
1	文件	打开	打开DXF文件，同时加载标注文件。	2
2	文件	保存	保存标注数据到文件中，建议存入DXF文件同途径同名的json文件中，便于下次打开。	3
3	文件	关闭	关闭当前文件。	2
4	文件	最近打开文件列表	记录最近打开的5个DXF文件，点击打开对应文件。	1
5	文件	退出	退出当前程序。	1
6	编辑	撤销	撤销上次编辑内容，可多次执行，直到恢复文件打开时状态。	3
7	编辑	重复	恢复上次撤销内容。	3
8	编辑	类型标注	1、进入选择CAD实体状态。
2、选中CAD实体（点选拉框选均可），记录实体与当前类型的关系。如果实体已与其他类型关联，则先取消之前的关联，再建立与当前类型的关系。
3、重复上面操作（过程中可通过类型面板切换当前类型），直到用户鼠标右键取消类型标注状态。	2
9	编辑	实例标注	1、进入选择CAD实体状态。原先已关联的CAD实体进入选中状态；
2、选中CAD实体（点选拉框选均可），记录实体与当前实例的关系。
3、重复上面操作（过程中可通过实例面板切换当前实例），直到用户鼠标右键取消实例标注状态。	2
10	视图	界面可见性控制	控制各个面板、工具栏、状态栏的可见性	1
11	视图	显示全图	缩放适当比例，使全部图元都能显示出来。	2
12	视图	实时缩放	入缩放状态，按住鼠标左键上移放大，下移缩小。	1
13	视图	窗口缩放	在绘图区点击两点，由这两点构成的矩形放大到整个窗口大小。	1
14	视图	实时平移	进入平移状态，按住鼠标左键平移整个矢量图，释放左键停止平移。	2
15	帮助	帮助	弹出帮助文档。	0.5
16	帮助	关于	弹出【关于】对话框。	0.5
17	界面	界面舶靠	工具栏、各类面板都能自由浮动或舶靠。	2
18	界面	类型面板	●新增：新增类型记录；
●删除：删除当前光标所在的类型记录；如果列表为空，应灰显不可用。
●上移：将当前类型与上面的类型记录交换位置；如果上面没有类型记录，应灰显不可用。
●下移：将当前类型与下面的类型记录交换位置；如果下面没有类型记录，应灰显不可用。	3
19	界面	实例面板	●新增：新增实例记录；
●删除：删除当前光标所在的实例记录；如果列表为空，应灰显不可用。	2
20	界面	图层面板	图层显隐控制	3
21	界面	状态栏	●打开的DXF文件路径；
●CAD实体数量；
●类型标注数量；
●实例标注数量；‘	1
22	界面	图形渲染区	1、DXF解析及渲染；
a)实体正确解析及渲染：
●点，直线、圆、圆弧（各1分）
●多段线（2分）
●文字、标注、INSERT 块参照（各3分）
b)正确解析组织结构，空间（模型空间、布局空间）、图层和块（各1分）
c)颜色渲染正确（2分）
d)线样式渲染正确（4分）
e)填充样式渲染正确（6分）
2、实体被选中时应高亮显示；（1分）
3、类型标注的实体颜色应变为类型设置的颜色；（2分）
4、实例标注不用改变实体颜色，而是绘制一个外接Box表示，线宽为2，颜色取类型颜色；（3分）	36
23	界面	界面关联	1、当前类型切换后，应同步刷新实例面板，只显示该类型下的实例；
2、选中实例节点后，将实例框缩放适当比例（充满屏幕的4/5），屏幕居中显示；	2
24	交互	鼠标交互	鼠标右键（大部分场景）、Esc键：第一次回到当前命令初始步骤，第二次终止当前命令；
鼠标左键：选择点或图元；
鼠标中键（滚轮）：放大、缩小；
鼠标中键按下：实时平移；
鼠标右键（前面有选择图元的步骤时）：表示确定选择图元，并继续执行命令下一步。	1
25	交互	捕捉图元	1、点选捕捉，当鼠标坐标距离图元很近时点击，将该图元选中；
2、正选拉框捕捉，从左上角往右下（上）角拉框，完全位于框内的图元被选中；
反选拉框捕捉，从右下角往左上（下）角拉框，只要和框相交的图元都被选中；	2
26	交互	鼠标光标样式	当命令处于不同状态时，建议改变光标给用户提示。以下是建议光标样式。
绘图区默认光标为十字光标；
捕捉点光标（未捕捉到）：

捕捉点光标（捕捉到）：

点选捕捉图元光标（未捕捉到）：

点选捕捉图元光标（捕捉到）：

实时平移光标：

实时缩放光标：
	1
共计80分